import React from "react";
const SideSubHeader = ({ title }) => {
  return (
    <div className="sidebarInnerContainer">
      <h6 className="sidebarText">{title}</h6>
    </div>
  );
};
export default SideSubHeader;
